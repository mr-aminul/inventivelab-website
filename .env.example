# SMTP — your own mail server (cPanel, Postfix, Microsoft 365, etc.)
SMTP_HOST=mail.inventivelab.bd
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@inventivelab.bd
SMTP_PASS=your-mailbox-password
SMTP_FROM=info@inventivelab.bd

# Where contact form submissions are delivered
CONTACT_TO=info@inventivelab.bd

# Local dev API port (optional)
CONTACT_API_PORT=8787
